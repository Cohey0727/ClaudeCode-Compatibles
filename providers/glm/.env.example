# GLM (Z.ai / Zhipu AI) settings. One file drives all three CLIs:
# Claude Code, OpenCode and pi. Every setting below is generic; the
# commented-out lines further down are per-CLI overrides you only need when
# one CLI should differ from the rest.
# Reference: https://docs.z.ai/devpack/tool/claude

# The launcher command is derived from this: claude<NAME>
# -> claudeglm.
NAME=glm

# Required: get an API key at https://z.ai/manage-apikey/apikey-list
API_TOKEN=

# Anthropic-compatible endpoint, shared by all three CLIs.
BASE_URL=https://api.z.ai/api/anthropic

# MODEL fills every main model slot, SMALL_MODEL every cheap one (Claude
# Code's haiku + subagent, OpenCode's small model, pi's second Ctrl+P entry).
MODEL=glm-5.3
SMALL_MODEL=glm-4.7

# Model limits: pi writes them into its generated models.json, and Claude
# Code takes CONTEXT_WINDOW as its auto-compact window. INPUT is "text" or
# "text,image"; REASONING says whether the models support extended thinking.
CONTEXT_WINDOW=1000000
MAX_TOKENS=131072
REASONING=true
INPUT=text

# glm-4.7 is a much smaller model than glm-5.3, so it gets its own limits.
SMALL_CONTEXT_WINDOW=200000
SMALL_MAX_TOKENS=131072

# Default CLI options prepended to every claude<NAME> launch (word-split).
# OpenCode and pi have no launcher, so this is Claude Code only.
ARGS=

# Claude Code extras, exported as-is.
CLAUDE_CODE_EFFORT_LEVEL=max

# --------------------------------------------------------------- overrides
# Everything below is optional — uncomment only to override the defaults
# derived from the settings above.

# Launcher command name. Default: claudeglm.
#COMMAND=claudeglm

# Default CLI options for claude<NAME>. Default: the value of ARGS.
#CLAUDE_ARGS=--permission-mode plan

# Token and endpoint for Claude Code. Default: API_TOKEN and BASE_URL.
#ANTHROPIC_AUTH_TOKEN=
#ANTHROPIC_BASE_URL=https://api.z.ai/api/anthropic

# Models per CLI. Default: MODEL (glm-5.3) for the main slots,
# SMALL_MODEL (glm-4.7) for the cheap ones. The opus / sonnet / fable
# slots follow ANTHROPIC_MODEL when it is set.
#ANTHROPIC_MODEL=glm-5.3
#ANTHROPIC_DEFAULT_HAIKU_MODEL=glm-4.7
#CLAUDE_CODE_SUBAGENT_MODEL=glm-4.7
#OPENCODE_MODEL=glm-5.3
#OPENCODE_SMALL_MODEL=glm-4.7
#PI_MODEL=glm-5.3
#PI_SMALL_MODEL=glm-4.7

# Claude Code's auto-compact window. Default: CONTEXT_WINDOW.
#CLAUDE_CODE_AUTO_COMPACT_WINDOW=1000000

# Marker appended to every Claude Code model slot (Claude Code-only syntax).
# Default: none. The 1M-context "[1m]" ids Z.ai documents for Claude Code
# exist only on the coding-plan route; BASE_URL above rejects them with
# "modelCode: does not exist", and glm-5.3 is already a 1M model there.
#CLAUDE_MODEL_SUFFIX=[1m]
